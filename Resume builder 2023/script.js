window.onresize = screen;
window.onload = screen;

function screen(){
    var myWidth = window.innerWidth;
        document.querySelector('#width').innerHTML = " width : " + myWidth + "";
    }
